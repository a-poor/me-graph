import React from "react";

/**
 * A table displaying the legend for the graph,
 * mapping node/edge types to colors.
 *
 */
export default function GraphLegend({}) {
  return <p>I'm the graph legend!</p>;
}
